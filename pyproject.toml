[project]
name = "health-agent"
version = "0.1.0"
description = "AI-powered daily meal planner powered by Claude Code"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk>=0.1.50",
    "python-dotenv>=1.0.0",
    "anyio>=4.0.0",
]

[tool.uv]
package = false
